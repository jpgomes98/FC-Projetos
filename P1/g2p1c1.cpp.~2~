#include <iostream>

using namespace std;

int main()
{
  int n_max=10;
  float r=.7, x=.5; // r eh o parametro geomehtrico, x eh o valor inicial
  int n = 0;
  
  while( n < n_max )
    { /* ciclo das iteradas */
      x *= a;
      cout << "x(" << ++n << ")= " << x << endl;
      n++;
      
    }
  return 0;

}
